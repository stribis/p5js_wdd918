// A p5js file always needs 2 functions
// setup ()
// Where initial values of the sketch come
function setup() {
    // create Canvas is used to create a 2D canvas in the website with a width and height
    createCanvas(400, 400);
}
// draw
// Where repeating values of the sketch come
function draw() {
    // background gives the canvas a background color
    background(213);
}